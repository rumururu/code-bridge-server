{
  "apiKey": "AIzaSy...",
  "projectId": "your-firebase-project-id",

  "_comment": "NOTE: Client Secret is NOT needed for Option 2 (app-only auth)",
  "_setup": [
    "1. Create a Firebase project at https://console.firebase.google.com",
    "2. Enable Authentication > Google Sign-In",
    "3. Enable Firestore Database",
    "4. Copy this file to firebase_config.json and fill in your values",
    "5. Set 'firebase_enabled: true' in config.yaml"
  ]
}
